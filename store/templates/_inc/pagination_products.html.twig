<div>
    <nav aria-label="Page navigation">
        <ul class="pagination">
            <li class="page-item"><a class="page-link" href="#">Previous</a></li>
            {% for i in range(nbProduct, 1) %}
                <li class="page-item"><a class="page-link" href="{{url('product.index', {'nbPage':i+1})}}">{{i}}</a></li>
            {% endfor %}
            <li class="page-item"><a class="page-link" href="#">Next</a></li>
        </ul>
    </nav>
</div>